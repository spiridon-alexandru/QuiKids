/**
 * These events are sent from the mein menu view to the controller when the user
 * clicks on a menu item.
 */
MainMenuEventTypes = {
		PushQuickScreen:0,
		PushPlayScreen:1,
		PushSettingsScreen:2,
		PushAchievementsScreen:3,
		PushHelpScreen:4,
		PushAboutScreen:5
}